apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml

configMapGenerator:
  - name: teleportal-demo-html
    namespace: teleportal-app
    files:
      - index.html
